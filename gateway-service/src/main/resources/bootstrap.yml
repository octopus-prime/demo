spring:
  application:
    name: gateway-service
  cloud:
    config:
      fail-fast: true
      retry:
        max-attempts: 10000
        max-interval: 5000
  output:
    ansi:
      enabled: always
#server:
#  port: 8079

---
spring:
  profiles: demo
  cloud:
    config:
      uri: http://configuration-service:8888
