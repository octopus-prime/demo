spring:
  application:
    name: depot-service
  jpa:
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:h2:mem:myDb;DB_CLOSE_DELAY=-1
  flyway:
    enabled: false
server:
  servlet:
    context-path: /kunde-api
