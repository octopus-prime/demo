server:
  port: 8761
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    shutdown:
      enabled: true
    health:
      show-details: ALWAYS
eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
