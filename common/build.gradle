dependencies {
    implementation(group: 'org.springframework', name: 'spring-webmvc')
    implementation(group: 'org.apache.tomcat.embed', name: 'tomcat-embed-core')
    implementation(group: 'org.springdoc', name: 'springdoc-openapi-core')
}

sonarqube {
    skipProject = true
}
